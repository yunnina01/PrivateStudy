#include <stdio.h>
#define MAX_SIZE 8

typedef struct{
    int queue[MAX_SIZE];
    int front;
    int rear;
}QueueType;

void init(QueueType *q){
    q->front = -1;
    q->rear = -1;
}

int is_empty(QueueType *q){
    
}
